
export interface category {
    id: string;
    name: string;
};

export interface color {
    id: string;
    name: string;
    value: string;
};

export interface size {
    id: string;
    value: string;
};